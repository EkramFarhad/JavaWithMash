package StringType;

public class Strings {

    public static void main(String[] args) {

        // string is also a referenceType and the actual type should be like this
        String myMessage = new String("the actual type should be like this ");


        String mySecondMessage = " hello all people";

        System.out.println(myMessage);

        System.out.println(mySecondMessage.toUpperCase());

        // string has a lot of method and like toUpperCase, trim, lowerCase and so On


    }
}
